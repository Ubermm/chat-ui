version: v1.0.0
variables:
  DOCKER_BUILDKIT: '1'
steps:
- cmd: export DOCKER_BUILDKIT=1
- cmd: docker build -t grimmy.azurecr.io/chatui2:latest . --build-arg BUILDKIT_INLINE_CACHE=1
  displayName: Build Docker image with BuildKit
  context: https://github.com/Ubermm/chat-ui.git
