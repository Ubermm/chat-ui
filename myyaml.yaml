version: v1.0.0
steps:
- cmd: ['buildx', 'build', '--platform', 'linux/amd64', '--tag', 'grimmy.azurecr.io/chatui2:latest', '--push', '--build-arg', 'BUILDKIT_INLINE_CACHE=1']
  displayName: Build Docker image using Buildx
  environment:
    DOCKER_BUILDKIT: '1'  # Set DOCKER_BUILDKIT environment variable to enable BuildKit
